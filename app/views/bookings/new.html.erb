<div class="container">
  <%= simple_form_for [@car, @booking] do |f| %>
  <%= f.input :start_on %>
  <%= f.input :end_on %>
  <%= f.submit "Create Booking", class: "btn btn-primary" %>
  <% end %>
</div>
